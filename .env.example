# =============================================================================
# Ambiente
# =============================================================================
# Ambiente de execução: development | production
APP_ENV=development

# Nível de log: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# =============================================================================
# API
# =============================================================================
# Porta do servidor FastAPI
API_PORT=8000

# =============================================================================
# Monitoramento
# =============================================================================
# Porta do Prometheus (métricas da API)
PROMETHEUS_PORT=9090

# Porta do Grafana (dashboards)
GRAFANA_PORT=3000

# =============================================================================
# MLflow Server
# =============================================================================
# Porta do servidor MLflow
MLFLOW_PORT=5001

# URL do servidor MLflow (cliente Python)
MLFLOW_TRACKING_URI=http://localhost:5001

# =============================================================================
# Modelo
# =============================================================================
# Caminho do artefato de modelo treinado
MODEL_PATH=models/model.joblib

# =============================================================================
# Modelo: backend de carregamento (joblib | onnx)
# =============================================================================
MODEL_BACKEND=joblib

# =============================================================================
# Kaggle
# =============================================================================
# Crie em https://www.kaggle.com/settings -> API -> Create New Token
# Baixe o kaggle.json e use os valores abaixo.
KAGGLE_USERNAME=
KAGGLE_KEY=

# =============================================================================
# DVC
# =============================================================================
# Remote DVC (OneDrive) — OPCIONAL. Deixe vazio para ambiente self-contained.
# O pipeline roda normalmente sem remote; ele só é necessário para
# compartilhar dados/modelos entre máquinas. Para configurar, preencha a URL
# e rode `make dvc-remote` (ou `make setup`).
#   macOS:   /Users/<usuario>/OneDrive - FIAP/techchallenge-fase3/dvcstore
#   Windows: C:/Users/<usuario>/OneDrive - FIAP/techchallenge-fase3/dvcstore
#   Linux:   /home/<usuario>/OneDrive/techchallenge-fase3/dvcstore
DVC_ONEDRIVE_REMOTE_URL=
